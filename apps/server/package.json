{
  "name": "@llm-wiki/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "LLM Wiki server — Fastify API + workers that reuse the src/lib core. Standalone sub-package (like mcp-server/); not part of the root workspace.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --target=node22 --packages=external --outfile=dist/index.js",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "migrate": "tsx src/db/migrate.ts"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/helmet": "^12.0.1",
    "@fastify/rate-limit": "^10.1.1",
    "@fastify/static": "^8.3.0",
    "fastify": "^5.8.5",
    "pg": "^8.13.1",
    "pg-boss": "^10.1.5",
    "pgvector": "^0.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.10",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
